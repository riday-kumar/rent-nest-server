enum ROLE {
    ADMIN
    LANDLORD
    TENANT
}

enum RentRequestStatus {
    PENDING
    APPROVED
    REJECTED
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    UNAVAILABLE
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    CANCELLED
}
